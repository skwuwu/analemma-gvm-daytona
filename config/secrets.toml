# API credentials for upstream services.
# Fill in your real tokens here — never commit real keys to git.
# Format: [credentials."hostname"]
#         type  = "Bearer" | "ApiKey"
#         token = "..."

# Example:
# [credentials."api.openai.com"]
# type  = "Bearer"
# token = "sk-..."
