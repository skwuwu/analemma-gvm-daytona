# API credentials for upstream services.
# GVM proxy holds these — the agent process never sees them.
# Agent env has no STRIPE_KEY. GVM injects post-enforcement.

[credentials."api.stripe.com"]
type  = "Bearer"
token = "sk_demo_not_real_replace_in_production"
